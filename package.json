{
  "name": "carveman",
  "module": "./dist/index.mjs",
  "main": "./dist/index.cjs",
  "type": "module",
  "private": true,
  "bin": {
    "carveman": "./dist/index.mjs"
  },
  "scripts": {
    "build": "bun build ./index.ts --target=node --format=esm --outdir=dist && bun build ./index.ts --target=node --format=cjs --outdir=dist",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}